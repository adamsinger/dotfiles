# Symlink to this file from ~/.zshrc
# You should also symlink ~/.zshenv to zsh/env in this directory
export PATH=/usr/local/bin:/usr/local/sbin:/usr/local/mysql/bin:/Developer/usr/bin:~/Library/Python/3.7/bin:/Applications/Postgres.app/Contents/Versions/latest/bin:$PATH

. ~/dotfiles/dotfiles/zsh/config
. ~/dotfiles/dotfiles/zsh/aliases
. ~/dotfiles/dotfiles/zsh/colors

# export GREP_OPTIONS=' --color=auto'
# export CLICOLOR=1
#
# export PATH="$HOME/.rbenv/bin:$PATH"
# eval "$(rbenv init -)"
if which rbenv > /dev/null; then eval "$(rbenv init -)"; fi
export PATH=$HOME/.rbenv/bin:$PATH

export PATH=$PATH:/Applications/Postgres.app/Contents/Versions/9.4/bin
export PATH=$PATH:/Applications/Visual\ Studio\ Code.app/Contents/Resources/app/bin
#export PATH=/usr/local/opt/python/libexec/bin:$PATH

export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"  # This loads nvm
[ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"  # This loads nvm bash_completion

export RUBYOPT='-W:no-deprecated -W:no-experimental'
